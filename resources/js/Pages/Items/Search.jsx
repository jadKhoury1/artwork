import Layout from "@/Layouts/Layout";

const Search = () => {
    return (
        <Layout>
            <div className="min-h-screen">
                <h2>Search Page Reached</h2>
            </div>
        </Layout>
    );
};

export default Search;